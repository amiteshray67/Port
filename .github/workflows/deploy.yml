name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 217.21.90.95
          username: u944949387
          password: Sphs@05164426082001
          # Correct web root on Hostinger — previous /public_html/ was wrong and deployed to FTP home dir
          server-dir: /domains/amiteshray.com/public_html/
          protocol: ftps
          port: 21
          timeout: 120000
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            readme.md
            temp_changes.html
            # Exclude large PDF notes and JPGs — deployed directly via FTP, too large for CI
            studyhub/assets/notes/*.pdf
            studyhub/assets/notes/*.jpg
            # Exclude seed_explainers.js — 12MB, deployed directly via FTP
            studyhub/assets/seed_explainers.js
            # Exclude skill/agent files — not part of website
            .agents/**
            .claude/**
